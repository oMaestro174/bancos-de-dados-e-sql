# Lista de ferramentas (bibliotecas) que o projeto precisa para funcionar.
# Para instalar todas de uma vez: pip install -r requirements.txt

Flask==3.1.3           # Cria o servidor web e monta as paginas (render_template)
PyMySQL==1.2.0         # Conecta o Python ao banco de dados MySQL
python-dotenv==1.2.2   # Lê a senha do MySQL de um arquivo secreto (.env)
