{
  "name": "consulta-alunos-iteam-root",
  "private": true,
  "scripts": {
    "postinstall": "cd app/backend && npm install && npx prisma generate",
    "build": "cd app/frontend && npm install && npm run build"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "dotenv": "^17.4.2",
    "express": "^5.2.1",
    "@prisma/client": "^5.22.0",
    "prisma": "^5.22.0"
  }
}
